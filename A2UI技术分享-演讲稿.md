<!--
 * @Author: hui.chenn
 * @Description: 
 * @Date: 2026-06-08 13:10:39
 * @LastEditTime: 2026-06-08 14:23:09
 * @LastEditors: hui.chenn
-->
# A2UI 驱动下前端开发实践 — 演讲稿

> 添可 AIT 中心 · 国内营销智能部 · 陈辉 · 2026.06

---

## 开场

各位领导好，我是来自国内营销智能部的陈辉，今天给大家分享的主题是"A2UI 驱动下的前端开发实践"。这次分享想让大家看到：我们团队在面对业务挑战时，如何从痛点出发、找到技术解法，并最终沉淀出可复用的能力。整个分享分四个部分，我按顺序展开。

---

## 第一部分：初探索 — 国内营销 APP

先从我们团队的背景说起。国内营销业务线多、并行任务多，开发资源相对紧张，运营侧又需要快速上线各种报表和活动页面。简单来说就是——**活多、人少、需求急**。

先从我们团队的背景说起。国内营销APP在年前2月份的时候，需要快速上线各种报表页面,但当时前端这边并行任务多，开发资源相对紧张。简单来说就是——**活多、人少、需求急**。

传统的模式呢，就是一张报表一个页面，如果开发多张报表，前端需要开发多个页面，开发的时间成本会增加。后来我们出了一个方案，依托珊瑚工作流生成 HTML 模板，再在端上渲染。这个方案能用，但有几个明显的瓶颈：

1. **交互被动**——HTML 渲染对多轮复杂交互的支撑比较弱，用户想微调一个状态，往往需要重新拉取或整体刷新页面。
2. **适配成本高**——iOS、Android、Web 三端对富文本和 HTML 标签的兼容性存在差异，调试非常繁琐。
3. **安全隐患**——直接在端上渲染注入的外部 HTML，存在 XSS 注入等安全风险。

这三个瓶颈让我们意识到：传统 HTML 模板方案已经到了天花板，我们需要一种更灵活、更安全、更智能的 UI 渲染方式。

---

## 第二部分：新挑战 — 运维小搭档桌面端

就在这个时候，运维小搭档这个项目来了。这个项目的诉求更加复杂——运维团队面临大量重复性工单和权限申请，问题排查依赖个人经验，知识难以沉淀，各系统之间信息割裂，AI 能力也无法以交互式界面直接触达用户。

业务侧提出了明确的需求：要灵活的表单、要智能的交互、要一个应用覆盖运维全场景。我们选择了 Vue 3 + Vite 6 + Element Plus + Electron 41 的技术栈来构建桌面端，但最关键的一个选型是——**A2UI Vue Engine 作为动态渲染引擎**。

为什么选 A2UI？因为它能实现"JSON Schema 驱动 + 零代码扩展"——后端或 AI 输出一段 JSON，前端就能实时渲染出可交互的界面。这正好解决了我们前面提到的 HTML 模板方案的所有痛点：交互更灵活、跨端一致、安全性更好。

---

## 第三部分：A2UI 使用进阶

说到 A2UI，可能有些领导还不太熟悉。A2UI 全称是"AI to UI"，是 Google 推出的一个开放协议。它的核心理念可以用两句话概括：

- **对话即界面**——用户通过自然语言与 AI 对话，AI 实时生成可交互的 UI。
- **界面即对话**——界面上的交互操作直接反馈到 AI 对话流中，形成闭环。

Google 在 2025 年 12 月首次公开发布 A2UI v0.8，采用 Apache 2.0 开源协议；2026 年 4 月又发布了 v0.9，新增了 React、Flutter、Angular 渲染器和 Python Agent SDK。目前 Google Opal 等生产系统已经落地应用。A2UI 的核心定位就是：**AI Agent 到用户界面的开放协议**。

我们在实际项目中的使用经历了几个阶段：

**阶段一**，输入一段话就能自动生成表单。比如用户说"我要申请网络权限"，AI 就能直接生成对应的表单界面。

**阶段二**，我们结合提示词工程，实现了更复杂的业务流程。以网络权限申请为例，我们把所有参数规则——上网权限、IM 权限、邮箱权限、USB 权限、VPN 权限等——都写进了提示词，大模型按规则收集信息，然后通过 A2UI 生成完整的申请表单，用户填完直接提交到 OA 系统。

这里我想强调一个关键认知：**大模型赋予了机器理解与生成的能力，但缺乏与用户交互的界面载体。A2UI 正是连接大模型与用户的桥梁**——让 AI 的输出不再局限于文本，而是直接成为可交互的界面。它带来了三个核心价值：

1. **规范化输出**——大模型按统一 Schema 生成 UI，确保一致性。
2. **即时渲染**——Schema 到界面毫秒级转化，零延迟响应。
3. **业务适配**——组件可扩展，灵活适配不同业务场景。

同时，我们已经把 A2UI 封装为独立的 npm 组件库并开源发布，任何项目都可以通过 `npm install` 直接引入，开箱即用、版本管控、跨项目复用。

在技术原理层面，a2ui-vue-engine 的核心架构有四大支柱：

1. **Schema 驱动渲染**——JSON Schema 声明式描述 UI，引擎解析渲染为 Vue 组件，AI 直接生成 JSON 即可产出界面。
2. **双格式 + 流式协议**——扁平格式对 AI 友好、树形格式对渲染友好，自动转换，JSONL 流式增量渲染，支持逐节点输出。
3. **声明式数据绑定**——支持 path、literal、expression 三种绑定类型和 transform 变换链，表单数据自动提取。
4. **组件映射 + 可扩展**——16 个内置组件加组件映射表，支持 registerComponent 动态注册自定义组件。

整个渲染流程是这样的：珊瑚系统生成一份 JSON，告诉前端“我要一个什么页面”；前端拿到这个 JSON 后，通过 A2UIRoot 接收，然后递归解析里面每一个节点，根据节点类型找到对应的 Vue 组件，把页面拼出来；用户点击、输入这些操作，再转换成 Action 事件，塞回聊天上下文，让 AI 继续处理。

---

## 第四部分：A2UI 能力封装

有了这些技术积累之后，我们思考的下一个问题是：**如何让其他同事也能用上这套能力？**

我们基于珊瑚平台提供了 A2UI SKILLS。具体来说，我们编写了两份核心规范文件：**SKILL.md** 和 **reference.md**。大模型读取这两个文件后，就能规划并生成符合规范的 A2UI JSON 配置。

- **SKILL.md** 定义了核心规范和使用指南，包括两种 JSON 格式（嵌套格式和推荐的扁平格式）、四大组件类（Layout、Form、Display、Action）、数据绑定规则、布局模式、交互配置，以及 10 条最佳实践。
- **reference.md** 则是组件 API 参考手册，覆盖 15 个组件、4 大分类，每个组件都有详细的 Props 表格和 JSON 代码示例。

最终，我们实现了**统一规范驱动，覆盖表单、展示、交互等全场景组件，赋能大模型高效生成前端 UI**。这意味着，任何接入珊瑚平台的业务线，只要配置好 A2UI SKILLS，大模型就能自动生成标准化的前端界面，无需前端开发介入。

---

## 总结

回顾整条线：我们从国内营销 APP 的开发痛点出发，发现传统 HTML 模板方案在交互、适配、安全上的瓶颈；运维小搭档项目带来了更复杂的挑战，推动我们选择了 A2UI 作为动态渲染引擎；在深入使用 A2UI 的过程中，我们从简单表单生成进化到复杂业务流程的提示词工程，理解了 A2UI 作为"大模型与用户之间桥梁"的核心价值；最后，我们把这套能力封装为 SKILL 规范和 npm 组件库，让其他团队也能零门槛使用。

一句话总结：**我们不是在做一个项目，而是在沉淀一种能力——让 AI 不只能说话，还能"画出"界面，让大模型的能力真正触达每一个业务场景。**

感谢各位领导聆听！
